---
"@moonshot-ai/kimi-desktop-tauri": minor
---

Add a 定时任务 (cron) section to Settings: list the active session's scheduled tasks with their cron expression, prompt, and last fire time, create new tasks via a guided form, and delete tasks — backed by the new session cron REST endpoints.
